# Language-specific information
language = 'en'
project = 'Medulla (English)'
copyright = '2024, Yvan Manon'
author = 'Yvan Manon'
release = '5.1'

# Build base directory (set based on language)
build_base = language  # 'en' for English build directory

# The `toctree` structure for English content
toctree = {
    'index': [
        'intro_en',  # Use separate source files for English content (optional)
        'installation_en',
        'usage_en',
        'contribute_en'
    ]
}

