version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  # Build each language in a separate directory (controlled by conf.py)
  build_base: .  # Uncomment this line if not using conditional build base in conf.py
