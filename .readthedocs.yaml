version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: conf.py  # Path to the base configuration file

python:
  install:
    - requirements: docs/requirements.txt  # Common requirements for both languages
