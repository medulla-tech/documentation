version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration:
    en: "docs/en/conf.py"  # English configuration file
    fr: "docs/fr/conf.py"  # French configuration file

python:
  install:
    - requirements: docs/requirements.txt  # Common requirements for both languages
